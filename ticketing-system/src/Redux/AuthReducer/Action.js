const init = {
  isAuth: false,
  token: "",
  isLoding: false,
  isError: false,
};

export const AuthReducer = (state = init, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
